on: [push]
name: Linyux_Deploy

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest
        steps:
        # checkout the repo
        - name: 'Checkout GitHub Action'
          uses: actions/checkout@main
          
        - name: 'Entering in the wooden door'
          uses: azure/login@v1
          with:
            creds: ${{ secrets.AZURE_CREDENTIALS }}
        
        - name: 'Building Minecraft House'
          uses: azure/docker-login@v1
          with:
            login-server: ${{ secrets.REGISTRY_LOGIN_SERVER }}
            username: ${{ secrets.REGISTRY_USERNAME }}
            password: ${{ secrets.REGISTRY_PASSWORD }}
        - run: |
            docker build . -t ${{ secrets.REGISTRY_LOGIN_SERVER }}/mc_mod
            docker push ${{ secrets.REGISTRY_LOGIN_SERVER }}/mc_mod

        - name: 'Pressing save game OwO'
          uses: 'azure/aci-deploy@v1'
          with:
          
            resource-group: ${{ secrets.RESOURCE_GROUP }}
            dns-name-label: femboycrusaders
            image: ${{ secrets.REGISTRY_LOGIN_SERVER }}/mc_mod
            registry-login-server: ${{ secrets.REGISTRY_LOGIN_SERVER }}
            registry-username: ${{ secrets.REGISTRY_USERNAME }}
            registry-password: ${{ secrets.REGISTRY_PASSWORD }}
            name: bigfattyfemboy
            azure-file-volume-account-key: Rup/HlPO+WdvJvwMj6zvFo7pTIG9h+Sq8fNDXRzCe6mjLDmGj78rKjHR3Bsy59ejccTgMj47VpfLsNuS83E82Q==
            azure-file-volume-account-name: bussybooty
            azure-file-volume-mount-path: /usr/src/mcserver/world
            azure-file-volume-share-name: acishare2
            memory: 6
            cpu: 2
            ports: 25565
            location: 'west europe'
